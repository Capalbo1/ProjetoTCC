{
  "name": "@tcc/api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend Node.js — regras de negócio, integrações de IA e operações privilegiadas que não devem ficar no cliente.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint ainda não configurado em apps/api)\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@tcc/shared": "*",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
